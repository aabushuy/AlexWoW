# .NET build output
**/bin/
**/obj/
**/out/
/publish/
publish/
*.user

# IDE / OS / editor мусор
.vs/
.idea/
.vscode/
Backup/
UpgradeLog.htm
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~

# Локальные секреты/конфиги
.env
appsettings.*.Local.json
.claude/settings.local.json
# реальный runner .env с PAT (шаблон — runner/.env.example)
runner/.env

# Клиент 3.3.5a и его данные — НЕ в git (Blizzard copyright, см. deploy/SECRETS.md).
# Генерируются игроком из своей копии WoW.
client/
*.MPQ
*.mpq
*.wtf
dbc/
maps/
vmaps/
mmaps/

# Локальные артефакты экстрактора (большие бинарники, не в репозиторий)
# data/ — хранимые данные (out-maps и т.п.); tests/extractor-output/ — пробные/тестовые выгрузки
data/
tests/extractor-output/
# Но бандл-данные DataStores (маленькие, встраиваются в сборку) — в репозитории.
!src/AlexWoW.DataStores/data/
!src/AlexWoW.DataStores/data/**

# Сгенерированные SQL-дампы экстрактора (tools/scripts/quest-test-reset.sql — рукописный, остаётся в репо)
tools/scripts/factiontemplate.sql
tools/scripts/out-charstartoutfit.sql
tools/scripts/charstartoutfit.sql
charstartoutfit.sql

# MCP-конфиг проекта содержит API-токены — в репозиторий не кладём
.mcp.json

# Конфиг Codex CLI — локальный, не коммитим
.codex/

# Локальный справочник протокола (gtker/wow_messages .wowm) — не в репозиторий
reference/

# Промежуточный список displayid для извлечения иконок (регенерится из БД, см. iconmap)
tools/MapExtractor/used-displayids.txt
tools/regression-import/__pycache__/
